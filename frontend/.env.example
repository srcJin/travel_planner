# Default: use this Next app's file-backed API.
# CRUD writes to src/lib/seed-data.json.

# Optional: point the frontend at the restored Flask backend.
# NEXT_PUBLIC_API_URL=http://127.0.0.1:5847

# Optional: enables map/geocoding features.
# NEXT_PUBLIC_MAPBOX_TOKEN=
